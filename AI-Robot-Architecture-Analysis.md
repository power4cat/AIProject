# AI-Robot 项目架构与技术栈分析

## 📋 项目概述

**AI-Robot** 是一个基于 **FastAPI + LangChain RAG + CrewAI 多智能体** 的智能客服系统，专门为 **MOTORLOAN.MY**（摩托车电商与融资平台）提供客服能力。项目采用 Python 3.11/3.12 开发，支持云端 API 和本地 Ollama 两种部署模式。

---

## 🏗️ 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    FastAPI 应用层                         │
│  /health | /api/v1/chat | /api/v1/chat/stream           │
│  /api/v1/ingest | /api/v1/stats | /api/v1/traces        │
│  /dashboard (可视化控制台)                                │
└─────────────────────────────────────────────────────────┘
         │                    │                    │
    ┌────┴────┐         ┌────┴────┐         ┌────┴────┐
    │ 限流中间件│         │ 请求日志  │         │ 异常兜底  │
    └─────────┘         └─────────┘         └─────────┘
         │
┌────────┴────────────────────────────────────────────────┐
│                    对话编排层                              │
│  ┌──────────────┐          ┌──────────────┐             │
│  │ 语义缓存检查  │ ──命中──>│ 直接返回答案   │             │
│  └──────────────┘          └──────────────┘             │
│         │未命中                                          │
│  ┌──────┴──────────────────────────────────────┐        │
│  │ 意图分类 (knowledge | order | chat)          │        │
│  └─────────────────────────────────────────────┘        │
│         │                                                │
│  ┌──────┴──────────────────────────────────────┐        │
│  │ CrewAI 多智能体 (优先) 或 LangChain 路由(降级)│        │
│  └─────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────┘
         │                    │                    │
    ┌────┴────┐         ┌────┴────┐         ┌────┴────┐
    │ RAG检索  │         │ 订单查询  │         │ 闲聊对话  │
    └─────────┘         └─────────┘         └─────────┘
         │
┌────────┴────────────────────────────────────────────────┐
│                    RAG 检索引擎                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │向量检索   │  │BM25检索   │  │RRF融合    │              │
│  └──────────┘  └──────────┘  └──────────┘              │
│                          │                              │
│                  ┌───────┴───────┐                      │
│                  │  bge-reranker │ (可选)                │
│                  └───────────────┘                      │
└─────────────────────────────────────────────────────────┘
         │
┌────────┴────────────────────────────────────────────────┐
│                    向量存储层                             │
│  InMemoryVectorStore (默认)  或  Milvus (持久化)         │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 目录结构

```
AI-Robot/
├── app/                          # 核心应用代码
│   ├── main.py                   # FastAPI 入口，路由定义
│   ├── config.py                 # 全局配置（环境变量驱动）
│   ├── schemas.py                # Pydantic 数据模型
│   ├── agents/                   # 智能体模块
│   │   ├── crew.py               # CrewAI 多智能体编排
│   │   └── tools.py              # Agent 工具集（RAG/订单/售后）
│   ├── rag/                      # RAG 检索增强生成
│   │   ├── retriever.py          # 知识库全链路（分块/索引/检索/生成）
│   │   ├── fusion.py             # RRF 融合算法
│   │   ├── lexical.py            # BM25 词面检索（jieba分词）
│   │   ├── reranker.py           # bge-reranker 重排
│   │   └── loader.py             # 文档加载器
│   └── services/                 # 服务层
│       ├── chat.py               # 对话编排（异步，CrewAI优先+降级）
│       ├── memory.py             # 会话记忆管理
│       ├── resilience.py         # 重试/退避机制
│       ├── semantic_cache.py     # 语义缓存
│       ├── ratelimit.py          # 限流中间件
│       └── tracing.py            # 链路追踪
├── data/                         # 知识库数据
├── eval/                         # RAGAS 评测
├── scripts/                      # 工具脚本（导入/评测）
├── tests/                        # 单元测试
├── docker-compose.yml            # Docker 编排配置
├── docker-compose-milvus.yml     # Milvus 向量库配置
├── Dockerfile                    # 容器化部署
├── requirements.txt              # 核心依赖
├── requirements-extra.txt        # 可选依赖（CrewAI/重排/Milvus）
└── requirements-eval.txt         # 评测依赖（RAGAS）
```

---

## 🛠️ 核心技术栈

### 1. Web 框架

- **FastAPI** (>=0.110) - 异步 Web 框架
- **Uvicorn** (>=0.29) - ASGI 服务器
- **sse-starlette** (>=1.6) - SSE 流式响应支持
- **python-multipart** - 文件上传支持

### 2. AI/LLM 框架

- **LangChain** (>=0.3.0) - LLM 应用框架
  - `langchain-openai` - OpenAI 兼容接口适配器
  - `langchain-text-splitters` - 文档分块
  - `langchain-milvus` (可选) - Milvus 向量库集成
- **CrewAI** (>=1.15.0, 可选) - 多智能体编排框架
  - 意图识别官 + 客服执行员双 Agent 协作
  - 未安装时自动降级为内置 LangChain 路由

### 3. 大模型支持

- **阿里云 DashScope** (通义千问 qwen-plus) - 推荐
- **DeepSeek** (deepseek-chat)
- **本地 Ollama** (qwen2.5:7b) - 离线部署
- **Embedding 模型**: text-embedding-v4 / nomic-embed-text

### 4. RAG 检索引擎

- **向量检索**: OpenAI 兼容 Embedding API
- **BM25 词面检索**:
  - `jieba` (>=0.42) - 中文分词
  - `rank_bm25` (>=0.2.2) - BM25Okapi 算法
- **RRF 融合**: Reciprocal Rank Fusion 算法融合双路检索结果
- **重排**: `sentence-transformers` (>=5.0) + `BAAI/bge-reranker-base` (可选)

### 5. 向量存储后端

- **InMemoryVectorStore** (默认) - 进程内零依赖
- **Milvus** (可选) - 持久化 ANN 向量库 (v2.5.4)

### 6. 稳定性工程

- **tenacity** (>=9.0) - 指数退避重试机制
  - 自动重试 429/5xx/网络错误
  - 最多 3 次尝试，最大退避 8 秒
- **语义缓存**: 余弦相似度 + 词面重叠双门限
- **限流**: 滑动窗口算法（按 IP，60 秒窗口，30 次/分钟）

### 7. 文档处理

- **pypdf** (>=4.2.0) - PDF 解析
- **python-docx** (>=1.1.0) - Word 文档解析
- **MarkdownHeaderTextSplitter** - Markdown 按标题层级分块
- **RecursiveCharacterTextSplitter** - 递归字符分块

### 8. 评测工具

- **RAGAS** - RAG 质量评测框架
- 自定义评测脚本: `eval/run_eval.py`

### 9. 部署与 CI/CD

- **Docker** - 容器化部署 (python:3.11-slim)
- **Docker Compose** - 服务编排
- **GitHub Actions** - CI/CD 流水线
  - 语法检查 → 离线单测 → RAGAS 评测（三阶段门禁）

---

## 🔑 核心设计亮点

### 1. 多层降级策略

```
CrewAI 多智能体 → LangChain 内置路由 → 异常兜底
```

- CrewAI 未安装或调用失败时自动降级
- 重排模型未加载时保留 RRF 融合顺序
- 所有降级对用户透明

### 2. 混合检索 + 重排

```
向量检索 + BM25 词面检索 → RRF 融合 → bge-reranker 重排
```

- 向量检索擅长语义理解
- BM25 擅长专有名词/订单号精确匹配
- RRF 无需归一化分数即可融合多路结果
- CrossEncoder 重排提升最终精度

### 3. 语义缓存优化

- **双门限机制**: 余弦相似度 ≥ 0.9 + 词面重叠率 ≥ 0.5
- **仅首轮问题参与**: 避免与会话记忆耦合
- **动态数据不缓存**: 订单查询等实时数据跳过缓存

### 4. 全链路可观测性

- **请求日志**: 记录每个请求的 HTTP 方法、路径、状态码、耗时
- **链路追踪**: 记录各阶段耗时（缓存/意图/检索/生成）
- **可视化控制台**: `/dashboard` 实时监控
- **性能指标**: P95 延迟、缓存命中率、限流拦截数

### 5. 会话记忆管理

- 按 `session_id` 隔离多轮对话
- 可配置最大保留轮次（默认 20 轮）
- 超出上限自动裁剪最旧消息
- 生产可替换为 Redis/SQLite 持久化

---

## 🚀 部署模式

### 模式 A: 云端 API（推荐）

```bash
docker compose up -d --build
```

- 使用阿里云 DashScope 通义千问
- 无需本地 GPU，开箱即用

### 模式 B: 本地 Ollama

```bash
# 修改 .env 指向 http://localhost:11434/v1
docker compose up -d --build
```

- 离线部署，数据不出境
- 需要本地运行 Ollama 服务

### 模式 C: 持久化向量库

```bash
docker compose -f docker-compose-milvus.yml up -d
# 设置 AIROBOT_VECTOR_STORE=milvus
```

- Milvus Standalone + 内嵌 etcd
- 支持知识库持久化

---

## 📊 性能特性

| 特性     | 配置                       | 说明                       |
| -------- | -------------------------- | -------------------------- |
| 文档分块 | 400 字符，重叠 80          | 平衡上下文完整性与检索精度 |
| 向量召回 | Top 5 (可配置)             | RRF 融合后取 Top 10        |
| 检索策略 | 向量 15 + BM25 15 → RRF 10 | 双路召回提升覆盖率         |
| 限流     | 30 次/分钟/IP              | 滑动窗口算法               |
| 重试     | 最多 3 次，指数退避        | 最大退避 8 秒              |
| 会话记忆 | 最多 20 轮，检索 6 轮      | 平衡上下文与 token 消耗    |

---

## 🎯 适用场景

1. **智能客服系统**: 电商、金融、教育等行业的问答机器人
2. **企业知识库**: 内部文档检索与问答
3. **RAG 技术验证**: 混合检索、重排、语义缓存等最佳实践
4. **多智能体学习**: CrewAI 编排模式参考实现

---

> **总结**: 这是一个**生产级别的 RAG 应用**，代码结构清晰，工程化程度高，非常适合作为学习参考或项目起点。
